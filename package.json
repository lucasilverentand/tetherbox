{
  "name": "local-linear-codex-bridge",
  "version": "0.1.0",
  "description": "Run local Codex App Server work from Linear agent sessions.",
  "type": "module",
  "private": false,
  "license": "MIT",
  "bin": {
    "local-linear-codex-bridge": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts serve --config examples/config.json",
    "test": "bun test",
    "check": "bun test && bun run scripts/check-format.ts"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "keywords": [
    "codex",
    "linear",
    "agent",
    "app-server"
  ]
}
